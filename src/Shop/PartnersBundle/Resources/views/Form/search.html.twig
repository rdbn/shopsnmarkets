{% extends 'UserUserBundle::layout.html.twig' %}

{% block title %}Поиск партнеров{% endblock %}

{% block content %}
    {{ include('ShopPartnersBundle:Partners:partnersMenu.html.twig') }}
    <div class="row top20">
        {{ form_errors(form) }}
        <div class="col-md-9">
            <div class="input-group">
                {{ form_errors(form.shopTags) }}
                {{ form_widget(form.shopTags) }}
                <span class="input-group-btn">
                        <button id="search" class="btn btn-default" type="button">
                            <span class="glyphicon glyphicon-search"></span>
                        </button>
                    </span>
            </div>
            <div id="result-search" class="top20"></div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                {{ form_errors(form.country) }}
                {{ form_widget(form.country) }}
            </div>
            <div class="form-group">
                {{ form_errors(form.city) }}
                {{ form_widget(form.city) }}
            </div>
            {{form_rest(form)}}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('public/js/location.js') }}" type="text/javascript"></script>
    <script src="{{ asset('bootstrap/js/search-partner.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/js/listMyShops.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/js/addPartners.js') }}" type="text/javascript"></script>
{% endblock %}